version: "3.9"
services:
  backend:
    build:
      context: ../
      dockerfile: backend/Dockerfile
    ports:
      - "8080:8080"
  frontend:
    build:
      context: ../
      dockerfile: frontend/Dockerfile
    ports:
      - "5000:5000"